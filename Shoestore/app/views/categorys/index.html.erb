<% if admin_signed_in? %>
  Logged in as <strong><%= current_admin.email %></strong>.
  <%= link_to 'Edit profile', edit_admin_registration_path, :class => 'navbar-link' %> |
  <%= link_to "Logout", destroy_admin_session_path, method: :delete, :class => 'navbar-link'  %>
<% end %>
<p id="notice"><%= notice %></p>
<h3>Category List</h3>
<td><%= link_to "New Category" , new_category_path %></td><br/>
<table>
  <tr>
    <th>Name</th>
    <th>Active</th>
    <th>Edit</th>
    <th>Delete</th>
  </tr>
  <% @category.each do |c|%>
  <tr>
  <td><%= link_to c.name  %></td>
  <td><%= c.active  %></td>
  <td><%= link_to 'Edit' , edit_category_path(c.id) %></td>
  </tr>
  <% end %>
</table>
<%= link_to 'Trang chu', admins_path %>